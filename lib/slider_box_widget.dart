import 'package:flutter/material.dart';

class SliderBox extends StatelessWidget {
  const SliderBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('슬라이더 박스 영역');
  }
}
